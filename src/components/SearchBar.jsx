import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input
        type="text"
        placeholder="Search"
        style={{ width: "100%", height: "30px" }}
      />
    </div>
  );
};

export default SearchBar;
